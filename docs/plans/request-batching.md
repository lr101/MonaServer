# Request reduction implementation plan

Spec: user asks to implement image URL reuse, completely remove web CORS preflights, and implement batching for all scrollable pages. Baseline e03c64f plus report c467035. Existing isolated worktree retained.

## Global constraints

Preserve existing API/mobile behavior, permissions, image cache identity and stale fallback. No insecure no-cors mode or credentials removal. Web uses same-origin /api through configurable reverse proxy; cross-origin third-party simple image GETs remain. Configure local E2E to exercise same-origin. Keep changes focused, use tests first, regenerate from sources, no generated hand edits. Follow all AGENTS.md. Each implementer owns only their task scope, is not alone, must not revert others, must not spawn subagents. Commit completed task. Read task report for integration interface. Run required tests and record results without credentials. Do not push or create PR; coordinator does this after final verification.

## Task 1: Bounded authenticated batch read API

Own api/, go-server/, flutter/api/ generated Dart only. Add POST /api/v3/batch, operationId batchRead, tag Batch, generated server and Dart client. Request {requests: [{kind, id}]} maximum 100 entries, minimum 1, unique processing where possible. Kinds: pinImage, userImageSmall, userImage, groupImageSmall, groupImage, groupPinImage, user, pinLikes. id UUID. Response {results:[{kind,id,status,imageUrl?,user?,likes?}]}; user references existing userInfoDto, likes existing pinLikeDto. Empty/malformed/over-limit/unknown-kind input yields 400; missing auth 401. Per-resource failures use status in results (no credentials, no data for errors), preserving existing endpoint authorization, soft-delete and caller-specific likes semantics. Do not turn hidden pin/group resources into public media. Reuse existing handlers/services in-process so no HTTP loopback calls; bounded sequential service reads acceptable in this step (web batching, not SQL optimization). Propagate context/cancellation, don't convert transient server errors into successful empty resources. Keep old individual endpoints unchanged. Generate Go embedded contract/server controllers and complete Dart client with pinned tools. Keep authoring fragments synchronized. Add meaningful failing then passing tests for route auth, malformed input/bounds, mixed accessible/forbidden resources, caller-specific likes, user and all image kinds against disposable PostGIS (may omit RustFS URL check if separate unit validated but report limitation). Coordinate constructor/router carefully. DB queries only if actually needed, with generators/tests if so.

## Task 2: Shared client hydration batching and supplied URL reuse

Own flutter/lib/ and Flutter unit/widget tests (not Docker, E2E, global_data_dto.dart). Consume generated BatchApi.batchRead contract from Task 1. Implement a session-scoped shared read coalescer: deduplicate keys, collect concurrent cache misses in a short bounded window, split at 100, bounded batch concurrency, propagate per-item errors, retryable failures must not become cached missing values, disposal/logout cancels pending work and prevents old-session cache writes. Test red/green batching distinct kinds/ids, duplicate consumers, splitting, errors, disposal. Route image-repository URL resolution for all six kinds, user metadata misses and pin-like misses through it, so every scrollable page using shared providers benefits (feed/map panel, group/profile gallery and image feed, rankings, groups/search/member lists, hidden-content lists). Audit other remote reads during row building and include if relevant or document why already page-fetched/local. Do not batch mutations. Feed paging must prefetch likes/user/URL metadata for the bounded next local page through the shared loader (not just rely on separately mounted cards to happen within one window), so slow scrolling benefits too. Retain lazy object-byte fetching; list metadata can be primed per server page without eager full-image downloads.

Retain supplied pin image URLs from sync and pin-list DTOs in a session-scoped registry independent of bytes (avoids DB schema migration). Route visible fetches through existing fetchImageFromUrl dedup/fallback. Register URLs before cache pin writes and ensure all consumers/prefetch use them; do not fetch every sync image eagerly. When absent/expired use batched URL resolution. Group normal sync prefetch must use cache-aware supplied URLs, reserving overrideUrl for explicit uploads. Use supplied small profile URLs in search/list/ranking data wherever available. Keep stable image byte streams, negative cache and stale bytes. Do not introduce permanent pin URL registry retention without bound.

Fix first-page-only group/user remote pin loading: walk bounded server pages to completion for metadata consumers requiring complete map/offline coverage, retain deletion handling and safe updatedAfter snapshot. Coalesce overlapping per-group refreshes and avoid reload due to irrelevant membership emissions; don't suppress actual visibility changes or public/empty refresh. Pagination UI already works on local metadata; retain lazy bounded media hydration. Guard against disposed async paging callbacks and stale search/ranking page responses where touched. Add coverage for >20 unjoined/user pins, immediate back-scroll cache reuse, concurrent widget/prefetch requests, metadata-only images, expired supplied URL fallback and failed refresh. Provide source-backed page coverage inventory and test results in report.

## Task 3: Same-origin browser API delivery

Own flutter/lib/data/dto/global_data_dto.dart (plus focused new API host resolver if needed), flutter/docker/, flutter/e2e infrastructure, scripts/dev proxy, mise.toml web tasks, docs stack configuration, relevant workflow tests. Browser API host must default/resolve to current origin regardless bundled mobile host so all auth/API calls use /api same origin. Preserve native dotenv/API_HOST behavior. Existing explicit web API backend setting should configure dev proxy rather than compiled cross-origin requests. Production nginx proxies /api/ to configurable API_UPSTREAM (default https://stick-it.lr-projects.de is existing backend); preserve URI/method/body/auth/status; suitable upload body limit, TLS SNI, no arbitrary user-controlled upstream/open proxy. Keep existing asset/SPA behavior. Docker template runtime config and clear deployment instructions. Local dev static+reverse proxy must use stdlib or existing tools, support GET/POST/PUT/DELETE and body/status; bind loopback and require safe configured backend; all E2E paths use it instead of plain static server. Test host selection, proxy routing including JSON errors/auth and non-API 404 assets. Test browser requests have zero API OPTIONS while login/mutations and reads succeed. Keep server CORS for legacy external consumers; max-age is not a substitute for same-origin.

## Task 4: Integrated request budgets and live verification

Own focused browser regression tests and updated report/verification documentation. Run full Flutter tests/analyze, Go vet/test and required integration suites, generation checks; build web and run live stack on disposable data. Use same-origin proxy. Navigate login/feed scroll/back, groups/search/detail/gallery, profile/gallery, user+group rankings/member pages; meaningful fixture with more than one page where supported. Assert zero API OPTIONS, no per-row individual image URL/user/like calls for covered reads, batches bounded and cache return-scroll reuse, expired URL fallback, no missing later-page content. Capture sanitized before/after counts; compare equivalent fixture/sequence. Test native host behavior via unit coverage. Complete final branch review, resolve important issues, create PR against develop. Do not deploy or merge.
