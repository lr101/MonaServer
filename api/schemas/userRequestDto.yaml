required:
  - email
  - password
  - name
type: object
properties:
  name:
    type: string
    maxLength: 256
    pattern: '^[^@]*$'
    description: Username. The @ character is reserved for email addresses.
  email:
    $ref: '../parameters/email.yaml'
  password:
    $ref: '../parameters/shortString.yaml'
