type: object
required:
  - userId
  - report
  - message
properties:
  userId:
    $ref: '../parameters/id.yaml'
  report:
    $ref: '../parameters/shortString.yaml'
  message:
    type: string
    maximum: 5000
  targetId:
    type: string
    format: uuid
    nullable: true
  targetKind:
    type: string
    maxLength: 32
    nullable: true
