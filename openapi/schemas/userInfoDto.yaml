type: object
required:
    - username
    - userId
    - tokens
properties:
  username:
    $ref: '../parameters/shortString.yaml'
  userId:
    $ref: '../parameters/id.yaml'
  tokens:
    $ref: '../schemas/tokenResponseDto.yaml'