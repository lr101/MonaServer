# Package the exact web output validated by CI; no Flutter compilation here.
FROM nginx:alpine
COPY docker/nginx.conf /etc/nginx/nginx.conf
COPY build/web/ /usr/share/nginx/html/
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
